

server:
  port: 8081
spring:
  application:
    name: springboot-demo
  datasource:
    url: jdbc:mysql://localhost:3306/demo?useSSL=false&characterEncoding=utf-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.ssli.springbootdemo.entity

book:
  name: Spring Boot Guide
  default-price: 88.0
  max-count: 100